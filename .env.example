# GitHub API令牌，用于留言板功能
# 需要在GitHub上创建一个具有repo权限的个人访问令牌
# 创建方法：GitHub > Settings > Developer settings > Personal access tokens > Tokens (classic)
GITHUB_TOKEN=your_github_token_here

# 留言板Issue配置
# 需要在GitHub仓库中创建一个用于存储留言的Issue，并在此处填入相关信息
ISSUE_REPO_OWNER=your_github_username
ISSUE_REPO_NAME=your_repo_name
ISSUE_NUMBER=1 